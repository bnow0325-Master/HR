# 근태관리 작업 인수인계

작성 기준일: 2026-08-06

## 1. 작업 목적

기존 프로젝트에 근태관리 기능을 추가하기 위한 출퇴근 및 연차 데이터 정리 작업을 진행했다. 현재 LIVECARE 운영 DB에 별도의 `CHECKINOUT_*` 테이블을 만들고, 엑셀 원장 데이터를 적재한 상태다.

## 2. 현재 반영 완료 사항

- LIVECARE DB 접속 확인 완료
- SQL Server 인스턴스 확인 완료: `db.livecareworld.com:1433`
- DB명: `LIVECARE`
- 기존 업무 테이블은 수정하지 않고 근태 전용 테이블을 신규 생성
- 출퇴근 데이터 `2,391건` 적재 완료
- 직원 데이터 `7명` 적재 완료
- 연차 잔액 데이터 `6명` 적재 완료
- 원장 기준일: `2026-07-21`
- 이세언의 `2026-07-21` 이후 출퇴근 기록은 제외

## 3. 이세언 퇴사 처리

- 로그인 ID: `su.lee@bnow.co.kr`
- 퇴사일: `2026-07-20`
- `IS_ACTIVE = false`
- `RESIGNED_ON = 2026-07-20`
- 적재된 이세언 기록: `585건`
- 이세언 기록 범위: `2025-02-20 09:18:00` ~ `2026-07-20 15:13:00`
- 연차 원장상 잔여 일수: `-1일`

> 법률·급여·퇴직금 계산은 별도 노무/세무 검토가 필요하다. 근태 시스템에서는 우선 실제 퇴사일과 마지막 출퇴근 기록을 분리해 저장하는 방향으로 처리했다.

## 4. 신규 테이블

### `dbo.CHECKINOUT_EMPLOYEE`

- `CHECKINOUT_EMPLOYEE_ID`
- `EMPLOYEE_CODE`
- `EMPLOYEE_NAME`
- `DEPARTMENT_NAME`
- `PIN_HASH`
- `IS_ACTIVE`
- `CREATED_AT`
- `UPDATED_AT`
- `LOGIN_ID`
- `HIRED_ON`
- `LEAVE_GROUP`
- `RESIGNED_ON`

### `dbo.CHECKINOUT_ATTENDANCE_RECORD`

- `CHECKINOUT_ATTENDANCE_ID`
- `CHECKINOUT_EMPLOYEE_ID`
- `RECORD_TYPE`
- `RECORDED_AT`
- `RECORD_METHOD`
- `IS_VERIFIED`
- `LATITUDE`
- `LONGITUDE`
- `NOTE`
- `CREATED_AT`

### `dbo.CHECKINOUT_ANNUAL_LEAVE_BALANCE`

- `CHECKINOUT_LEAVE_BALANCE_ID`
- `CHECKINOUT_EMPLOYEE_ID`
- `BASE_YEAR`
- `GRANTED_DAYS`
- `USED_DAYS`
- `ADJUSTED_DAYS`
- `UPDATED_AT`

추가 인덱스:

- `IX_CHECKINOUT_ATTENDANCE_EMPLOYEE_RECORDED_AT`
- `IX_CHECKINOUT_ATTENDANCE_RECORDED_AT`
- `IX_CHECKINOUT_LEAVE_EMPLOYEE_YEAR`

## 5. 원본 및 프로젝트 파일

- 원본 연차 파일: `C:\Users\Elon Choo PC\Downloads\annualLeaveList.xlsx`
- 원본 출퇴근 파일: `C:\Users\Elon Choo PC\Downloads\commuteList.xlsx`
- 시드 생성기: `sql\generate_checkinout_seed.py`
- 생성된 시드 SQL: `sql\checkinout_seed.sql`
- 컬럼 준비 SQL: `sql\checkinout_prepare.sql`
- 스키마 확인 SQL: `sql\checkinout_schema_verify.sql`
- 검증 스크립트: `sql\checkinout_verify.ps1`

## 6. Alpacon 접속 정보

접속 가이드는 `C:\Users\Elon Choo PC\Downloads\alpacon-codex-guide.md`와 `D:\project\bnow-0325\CLAUDE.md`를 기준으로 했다.

- Alpacon 실행 파일: `C:\Users\Elon Choo PC\AppData\Local\alpacon\alpacon.exe`
- DB 서버: `5-backend-db`
- Windows 서버 사용자 옵션: `-u Administrator`
- 사용한 작업 세션: `69064f35-2c67-4c26-9bce-e580f51df001`
- 서버에서 업로드 파일 경로: `C:\checkinout_seed.sql`

예시:

```powershell
alpacon server ls
alpacon exec --wait --work-session 69064f35-2c67-4c26-9bce-e580f51df001 -u Administrator 5-backend-db -- powershell -ExecutionPolicy Bypass -File C:\checkinout_verify.ps1
```

## 7. 반드시 알아둘 제한 사항

- 현재 프로젝트의 앱 기술 스택은 Prisma 7 + PostgreSQL을 전제로 한다.
- LIVECARE 운영 DB는 이번 작업에서 SQL Server로 확인됐다.
- 따라서 현재 신규 `CHECKINOUT_*` 테이블은 DB에 적재됐지만, 앱의 Prisma 스키마/API 화면과 아직 연결되지 않았다.
- 다음 작업은 SQL Server 연결 방식 또는 별도 근태 DB 연결 전략을 결정한 후 진행해야 한다.
- 기존 LIVECARE DB 전체 백업은 서버 디스크 용량 또는 권한 문제로 성공하지 못했다. 운영 반영 전 별도 백업 확보가 필요하다.
- DB 비밀번호 등 민감정보는 문서나 Git에 기록하지 않는다.
- 현재 작업 트리에는 이전 작업으로 인한 변경 파일과 임시 파일이 있으므로, 커밋 전 `git status`와 변경 내용을 확인해야 한다.

## 8. 다음 개발 작업

1. 근태관리 메뉴를 앱의 단일 메뉴로 추가한다.
2. 근태관리 화면에서 직원별 일자별 출근·퇴근 기록을 조회한다.
3. 연차 잔여일수와 사용일수를 직원별로 표시한다.
4. 관리자 인증 후 직원 상태와 퇴사일을 수정할 수 있게 한다.
5. SQL Server 신규 테이블을 앱에서 조회할 연결 계층을 결정한다.
6. `2026-07-21` 기준 데이터와 이세언 퇴사일 처리에 대한 관리자 검증 화면을 추가한다.
7. 실제 출퇴근 입력 시 PIN + GPS + 동적 QR 검증을 적용한다.
8. 테스트와 `npm run check`를 수행한 뒤 기능 브랜치와 PR로 배포한다.

## 9. 다음 채팅에 전달할 한 줄 요약

> LIVECARE SQL Server에 `CHECKINOUT_*` 근태 테이블 3개를 신규 생성하고 2026-07-21 기준 출퇴근 2,391건과 연차 데이터를 적재했다. 이세언은 2026-07-20 퇴사자로 비활성 처리했으며, 이제 프로젝트의 근태관리 단일 메뉴에서 이 테이블을 조회·관리하는 앱 연동 작업이 필요하다.
